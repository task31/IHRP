# Claude Code local session files — never commit
.claude/settings.local.json

# Local deploy credentials — never commit
.deploy.env

# Cursor local cache files — never commit
*.mdc.txt

# MySQL local data directory
mysql-data/

# Local smoke/debug scripts
smoke_*.py
debug_*.py

# Python bytecode (e.g. root deploy.py)
__pycache__/
*.py[cod]

# Screenshot / smoke test artifacts
login_*.png
smoke_screenshots/

# SSH keys — never commit
Keys/
