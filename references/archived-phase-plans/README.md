# Archived phase plans

Completed `phase-*-plan.md` files from the repo root (see TASKLIST T024).  
For new work, the Architect still creates the active plan as `phase-N-plan.md` at the repository root per workflow rules.
